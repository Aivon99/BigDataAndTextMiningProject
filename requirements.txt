# Core deep learning stack
torch
torchvision
torchaudio
transformers>=4.45.0
accelerate
torchao
peft
qwen-vl-utils

# Data / Hugging Face Hub
datasets
huggingface_hub
python-dotenv
PyYAML

# Chess rendering & serialization
chess
cairosvg
# NOTE: Task_1.ipynb's Qwen eval pipeline needs Pillow<11.0.0 specifically.
# That cap has no prebuilt wheel on newer Python versions (e.g. 3.13/3.14),
# forcing a from-source build; left unpinned here for the general/data-gen
# path, pin it back down manually if you're running that notebook's env.
Pillow

# Data wrangling & metrics
pandas
numpy
scikit-learn
python-Levenshtein
zstandard

# Utilities / plotting
tqdm
matplotlib
